# OpenClaw image to run (change if you prefer a specific tag/version)
OPENCLAW_IMAGE=ghcr.io/openclaw/openclaw:latest

# OpenClaw host port (UI is exposed on http://localhost:${OPENCLAW_PORT})
OPENCLAW_PORT=3000

# Ollama model auto-pulled at startup
OLLAMA_MODEL=gemma4:e4b
